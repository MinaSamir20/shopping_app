import 'package:flutter/material.dart';

class ShoppingLayout extends StatelessWidget {
  const ShoppingLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
